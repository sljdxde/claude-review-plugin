{
  "name": "codex-claude-review-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Codex plugin that runs read-only Claude Code reviews for working tree and branch diffs.",
  "license": "MIT",
  "keywords": [
    "codex",
    "claude",
    "plugin",
    "review",
    "code-review"
  ],
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
